# Normalize all text to LF in the repo and on checkout (matches Prettier's
# default and keeps diffs clean across the Linux + Windows CI matrix).
* text=auto eol=lf

# Binary assets — never normalize.
*.png binary
